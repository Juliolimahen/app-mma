import React from 'react';
import { NavbarContainer } from './styles';

const Navbar: React.FC = () => {
    return (
        <NavbarContainer>
            <h2>MMA Stats</h2>
        </NavbarContainer>
    );
};

export default Navbar;
